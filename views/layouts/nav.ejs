<nav class="navbar" id="nav">
  <a id="hamburger-menu"><i data-feather="menu"></i></a>

  <div class="navbar-nav">
    <a href="#home">Home</a>
    <a href="#grid-container">Product</a>
    <a href="#contact">Contact</a>
    <% if (emailN === "admin@gmail.com") { %>
    <a href="/admin/dashboard">Dashboard</a>
    <% } %>
  </div>
  <a href="/" class="navbar-logo">COOLNIQLO</a>
  <div class="navbar-extra">
    <div class="xSearch">
      <a href="javascript:void(0)" id="searchToggle">
        <i data-feather="x" id="searchIcon" style="display: none" href="javascript:void(0)"></i>
        <i data-feather="search" id="closeIcon"></i>
      </a>

      <div id="searchBar" class="formSearch">
        <input type="text" class="search" id="searchInput" name="q" placeholder="Search..." oninput="searchProducts(this.value)" />
        <button type="submit" class="submitBtn" onclick="clearSearch()">Clear</button>
      </div>
      <div>
        <% if (emailN != null) { %>
        <form action="/logout?_method=DELETE" method="POST" class="logout">
          <button type="submit">Logout</button>
        </form>
        <% } else { %>
        <a href="/login" class="logout">Sign In</a>
        <% } %>
      </div>

</nav>